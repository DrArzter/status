# Copy to terraform.tfvars, which is ignored. The token is read from the
# environment in CI instead: TF_VAR_cloudflare_api_token.
account_id = "your-cloudflare-account-id"
zone_id    = "zone-id-of-drarzter-dev"
hostname   = "status.drarzter.dev"
